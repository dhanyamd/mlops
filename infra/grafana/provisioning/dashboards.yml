apiVersion: 1

providers:
  - name: "ML Model Health"
    orgId: 1
    folder: "MLOps"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30   # Hot-reload dashboards without restarting Grafana
    allowUiUpdates: true
    options:
      path: /etc/grafana/dashboards
